<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="fr.ldnr.guillaume.zoo">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:icon="@drawable/icone">
        <activity android:name=".HomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".MapActivity" android:label="@string/map_title"/>
        <activity android:name=".AquariumActivity" android:label="Aquarium Map"/>
        <activity android:name=".PopCornActivity" android:label="Popcorn"/>
        <activity android:name=".AlertActivity" android:label="@string/alert_title"/>
    </application>
</manifest>
